name: Mirror to Account A

on:
  push:
    branches:
      - main

jobs:
  git-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Push to Account A
        run: |
          git remote add destination https://${{ secrets.Vercel_Sync }}@github.com/Mediceberg/Reputa-Analytics.git
          git push -f destination main 
